<UserControl
    x:Class="ImgPlacer.Views.Controls.XElementInputPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ImgPlacer.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:XElementInputPanelViewModel}"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Input Xml Element:" />

        <TextBox
            Grid.Row="1"
            Margin="0,8"
            Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" />

        <Button
            Grid.Row="2"
            HorizontalAlignment="Right"
            Command="{Binding ApplyImageInfoCommand}"
            Content="Apply (Ctrl + Enter)" />
    </Grid>
</UserControl>