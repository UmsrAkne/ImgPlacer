<UserControl
    x:Class="ImgPlacer.Views.Controls.CopyHistoryList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:ImgPlacer.Utils.Behaviors"
    xmlns:controls="clr-namespace:ImgPlacer.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ImgPlacer.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:CopyHistoryListViewModel}"
    mc:Ignorable="d">

    <ListBox Height="400" ItemsSource="{Binding CopyHistories}">
        <i:Interaction.Behaviors>
            <behaviors:BubbleMouseWheelBehavior />
        </i:Interaction.Behaviors>

        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type viewModels:ImageCanvasViewerViewModel}">
                <ContentControl>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.RestoreHistoryCommand}" CommandParameter="{Binding}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <controls:ImageCanvasViewer
                        Width="{Binding CanvasWidth}"
                        Height="{Binding CanvasHeight}"
                        Margin="0"
                        DataContext="{Binding}">
                        <controls:ImageCanvasViewer.LayoutTransform>
                            <ScaleTransform ScaleX="0.1" ScaleY="0.1" />
                        </controls:ImageCanvasViewer.LayoutTransform>
                    </controls:ImageCanvasViewer>
                </ContentControl>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>